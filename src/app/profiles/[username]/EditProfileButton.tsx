'use client';

// Components.
import { Button } from '@/components/Button';

// Component.
export default function EditProfileButton() {
  return <Button outline small label="Edit Profile" onClick={() => {}} />;
}
